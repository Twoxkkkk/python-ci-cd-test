name: Test
on: 
  push:
    branches:
      - master
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install dep
        run: uv lock --upgrade && uv sync
      - name: Tests
        run: uv run pytest
      
